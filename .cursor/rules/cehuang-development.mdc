---
description: 测谎项目开发：验证后交付、最佳实践、模块解耦
alwaysApply: true
---

# 测谎系统 - 开发与交付规则

## 验证后再交付

- 所有交付物在合并/交付前必须经过验证。
- 至少做到：相关自动化测试通过（若有）；关键路径手工或脚本校验（如创建会话 → 发帧 → 收结果 → 结束会话 → 获取报告）。
- 涉及 Qwen 语义时，确认本地 `qwen2_5_7b_server` 已启动且 `POST /chat` 可调通。
- 交付后在本条迭代的 `docs/PROGRESS.md` 中记录完成项与验证方式。

## 最佳实践与不过度抽象

- 命名清晰、职责单一；优先可读与可维护，不引入不必要的抽象（如无需求的“通用框架”）。
- 配置从环境/配置文件读取；依赖在 `requirements.txt` 中固定版本。
- 关键路径有错误处理与日志；调用外部服务（如 Qwen）时处理超时与不可用。

## 模块解耦

- 每个功能模块尽量解耦，便于单独测试与后续迭代。
- 模块间通过明确接口交互（函数签名、HTTP、消息格式）；不依赖实现细节。
- 后端：各 `services/*` 仅通过入参/返回值与融合引擎交互；Qwen 语义仅通过 HTTP 调本地服务，不在本仓库内加载模型。详见 `docs/TECH.md` §3。
