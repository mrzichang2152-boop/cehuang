---
description: 测谎项目规范：进度文档、防污染、需求变更同步、AI 友好
alwaysApply: true
---

# 测谎系统 (cehuangxitong) 项目规则

## 进度与文档防污染

- **唯一进度文档**：`docs/PROGRESS.md`。每次功能交付、重要修复或需求变更后，必须在此追加一条记录（日期、类型、简要说明、涉及模块）。
- **禁止**：在项目根、`backend/`、`frontend/` 下新增与业务无关的 .md（如“今日总结”“AI 草稿”）；禁止新建其他“进度”“日志”类文档替代 `docs/PROGRESS.md`。
- **脚本**：可执行脚本统一放在 `scripts/`，命名清晰；临时脚本用毕删除或移入 `scripts/` 并在 `docs/PROGRESS.md` 中说明。
- **AI 生成文档**：说明性 .md 只允许放在 `docs/`，且需在 `docs/PROGRESS.md` 中记一笔；不得在源码目录下新增“说明.md”“笔记.md”等。

## 需求变更时的文档同步

- 当**需求发生变动**时：
  1. 先更新 `prd/PRD.md`（功能、页面、接口、数据模型等）。
  2. 再更新 `docs/TECH.md`（技术选型、目录、模块边界、部署、验证清单）。
  3. 最后在 `docs/PROGRESS.md` 中记录本次需求变更及 PRD/TECH 修改点。
- 在改代码前完成上述文档更新。

## 对 AI 的友好约定

- 需求与规范以 `prd/PRD.md`、`docs/TECH.md` 为权威来源；实现时优先引用其中章节（如“见 PRD §四”“见 TECH §3”）。
- 文档使用清晰标题、列表和表格；接口与数据模型用 JSON/YAML 示例或表格描述，便于解析与生成代码。
